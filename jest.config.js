module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.test.ts'],
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
  collectCoverageFrom: [
    'src/utils/**/*.ts',
    'src/constants/**/*.ts',
    '!src/**/*.d.ts',
    // Exclude React Native specific utilities that can't be tested in Node
    '!src/utils/haptics.ts',
    '!src/utils/platform.ts',
  ],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80,
    },
  },
};

